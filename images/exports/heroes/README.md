# Hero QA Exports

Per-template hero previews should be saved as:

```text
images/exports/heroes/{slug}-tNN-hero.png
```

These are kept for side-by-side review. The canonical upload file remains `images/exports/{slug}-hero.png`.

